generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env.example("DATABASE_URL")
}

model Company {
  id                 Int       @id @default(autoincrement())
  name               String
  registrationNumber String
  services           Service[]
}

model Service {
  id          Int     @id @default(autoincrement())
  companyId   Int
  name        String
  description String
  price       Float
  company     Company @relation(fields: [companyId], references: [id])
}
